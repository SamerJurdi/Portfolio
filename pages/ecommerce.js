
import {Layout} from '../components'

export default function user() {
	// Check if there is an active user through an API call
	// If there is, get there data
	return (
		<Layout>
			<div>E-commerce</div>
		</Layout>
	)
}